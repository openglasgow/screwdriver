% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_character.R
\name{check_character}
\alias{check_character}
\title{Check Character Vector Validity}
\usage{
check_character(
  x,
  length_range = NULL,
  allowed_values = NULL,
  num_chars = NULL,
  allow_NA = TRUE,
  x_name = "Character vector"
)
}
\arguments{
\item{x}{An atomic character vector to be validated.}

\item{length_range}{A single positive whole number or a vector of length 2 indicating
the exact, minimum, or maximum length of \code{x}. Use \code{NA} to omit the minimum
or maximum constraint.}

\item{allowed_values}{A character vector specifying the allowed values for elements in \code{x}.
If \code{NULL}, this check is skipped.}

\item{num_chars}{A single positive whole number or a vector of length 2 indicating
the exact, minimum, or maximum number of characters for each element in \code{x}.
Use \code{NA} to omit the minimum or maximum constraint.}

\item{allow_NA}{A logical value indicating whether \code{NA} values are allowed in \code{x}.}

\item{x_name}{Optional character string specifying the name of the vector being checked.
Used in error messages for clarity.}
}
\value{
\code{TRUE} if \code{x} passes all specified checks, otherwise
an error is thrown detailing the first encountered discrepancy.
}
\description{
Performs validations on an atomic character vector, including length, allowed values,
character count within elements, and allowance of NAs.
}
\examples{
check_character(c("apple", "banana"), length_range = 2, allowed_values = c("apple", "banana", "cherry"),
                num_chars = c(5, NA), allow_NA = FALSE, x_name = "fruit vector")

}
